# Coding harness

**Composition:** Pi + Qwen + filesystem/search/shell/edit tools + completion verifier + coding prompt.

Use it for bounded repository work. Pi owns the loop and tool execution; the verifier records a receipt before accepting a file-based completion claim. Shell access makes policy and task scoping important.
