# Researcher harness

**Composition:** Pi + Qwen + read-only retrieval tools + evidence-summary skill + evidence-first prompt.

Use it to gather and synthesize local sources. Add a network retrieval tool at the capability layer when needed. Its main failure is a synthesis that outruns the retrieved evidence.
