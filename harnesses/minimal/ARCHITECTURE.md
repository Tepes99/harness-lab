# Minimal harness

**Composition:** Pi + Qwen + a short system prompt. No tools, skills, extensions, context files, or persisted session.

Use it as the behavioral baseline. Its single model call isolates prompt/model behavior, but it cannot inspect evidence or act on the workspace.
