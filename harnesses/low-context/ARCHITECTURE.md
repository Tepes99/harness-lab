# Low-context harness

**Composition:** ephemeral Pi + Qwen + only `read` + a terse prompt. Skills, discovery, context files, extensions, and session persistence are disabled.

Use it for narrow tasks where startup context would dominate. The tradeoff is less repository knowledge and no cross-turn continuity.
