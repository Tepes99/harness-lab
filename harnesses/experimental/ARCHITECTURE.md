# Experimental harness

**Composition:** Pi + Qwen + context inspector + one explicitly selected experimental extension or prompt.

Use it for strange architectures whose behavior is still uncertain. Keep each run ephemeral and observable so failed ideas do not become hidden defaults.
